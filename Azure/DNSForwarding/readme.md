The Script "CreatePrivateEndpointDNSForwardings.ps1" could be used to generate another PowerShell Script which then will create Conditional Forwarding for given DNS Zones and target Master Servers on any Windows-Server-based DNS-Server. This could be used to create the needed forwarding for Private Endpoints on Azure on the on-premises DNS Servers to an given DNS-forwarder in Azure.